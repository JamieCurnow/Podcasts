---
description: Create a catalogue of mongo queries used in the app for help with indexing
---

look through all of the server routes and utils, and create a markdown file that lists all of the mongoDb queries that we do in the app. Any time we interface with mongodb we use the `useMongoDb()` util so you can use that for the starting point of your search.

I want a list of all of the queries so that we can analyze them and see what indexes need to be made. Maintain this list in MONGO_QUERIES.md
